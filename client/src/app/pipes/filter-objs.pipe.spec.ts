import { FilterObjsPipe } from './filter-objs.pipe';

describe('FilterObjsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterObjsPipe();
    expect(pipe).toBeTruthy();
  });
});
